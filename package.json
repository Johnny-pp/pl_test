{
  "name": "pl_test",
  "version": "1.0.0",
  "description": "基于 Phaser 4 的原创网页幻兽养成游戏",
  "scripts": {
    "validate": "node scripts/validate-pals.mjs",
    "dev": "vite",
    "build": "vite build",
    "test": "node --test --experimental-strip-types tests/*.test.ts",
    "test:browser": "npm run build && node scripts/browser-smoke.mjs",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src tests scripts/*.mjs vite.config.mjs eslint.config.js",
    "format": "prettier --write 'src/**/*.ts' 'tests/**/*.ts' 'scripts/**/*.mjs' '*.mjs' '*.js'",
    "format:check": "prettier --check 'src/**/*.ts' 'tests/**/*.ts' 'scripts/**/*.mjs' '*.mjs' '*.js'",
    "check": "npm run format:check && npm run lint && npm run typecheck && npm test && npm run validate && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "ajv": "^8.20.0",
    "phaser": "^4.2.1",
    "vite": "^8.2.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.1",
    "globals": "^17.11.0",
    "prettier": "^3.9.6",
    "typescript": "^6.0.2",
    "typescript-eslint": "^8.67.0"
  }
}
